services:
  db:
    image: postgres:latest
    container_name: d20-warhammering-dev-db
    restart: unless-stopped
    ports:
      - '5432:5432'
    env_file:
      - .env
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
