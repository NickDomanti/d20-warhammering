POSTGRES_URL=
ADMIN_PASSWORD=

# Netlify Blobs (manual mode). Required when running outside of Netlify (e.g. local dev, Vercel).
# When deployed to Netlify these are injected automatically and can be left empty.
NETLIFY_SITE_ID=
NETLIFY_BLOBS_TOKEN=